/* eslint-disable prettier/prettier */
import { AbstractModel } from "../common/abstract.model"
import { ResponseData } from "./response-data.model";

/* eslint-disable prettier/prettier */
export class AirQualityModel extends AbstractModel{
     data: ResponseData;
}